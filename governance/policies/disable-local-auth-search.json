{
  "properties": {
    "displayName": "AI-LZ: Azure AI Search services must use Entra-only authentication",
    "policyType": "Custom",
    "mode": "Indexed",
    "description": "Denies Microsoft.Search/searchServices that do not set disableLocalAuth = true, enforcing Microsoft Entra ID authentication instead of admin API keys.",
    "metadata": { "category": "Search", "version": "1.0.0" },
    "parameters": {
      "effect": {
        "type": "String",
        "allowedValues": [ "Audit", "Deny", "Disabled" ],
        "defaultValue": "Deny",
        "metadata": { "displayName": "Effect" }
      }
    },
    "policyRule": {
      "if": {
        "allOf": [
          { "field": "type", "equals": "Microsoft.Search/searchServices" },
          { "field": "Microsoft.Search/searchServices/disableLocalAuth", "notEquals": true }
        ]
      },
      "then": { "effect": "[parameters('effect')]" }
    }
  }
}
